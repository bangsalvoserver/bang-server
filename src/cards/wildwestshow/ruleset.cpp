#include "ruleset.h"

#include "game/game.h"

namespace banggame {

    void ruleset_wildwestshow::on_apply(game *game) {
        // TODO
        // add listener on_effect_end
        // if card has_tag(changewws) and card != last_played_card
        // move wws scenario deck
        // change wws card if present
        // on_disable old card if present
        // on_enable new card
    }
}